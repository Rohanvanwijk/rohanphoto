const state = {
    showAboutMe: false
};

const getters = {
    aboutMe: (state) => state.showAboutMe
};

const actions = {};

const mutations = {};

export default {
    state,
    getters,
    actions,
    mutations
}
